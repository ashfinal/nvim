```bash
yay -S swift-bin nerd-fonts-sarasa-term --needed --noconfirm
sudo pacman -S fzf ripgrep --needed --noconfirm
sudo pacman -S lua-language-server gopls rust-analyzer clang deno tombi --needed --noconfirm
sudo npm install -g @olrtg/emmet-language-server @tailwindcss/language-server pyright svelte-language-server typescript typescript-language-server vscode-langservers-extracted yaml-language-server prettier
```

```bash
git clone --depth=1 git@github.com:ashfinal/nvim ~/.config/nvim
```
